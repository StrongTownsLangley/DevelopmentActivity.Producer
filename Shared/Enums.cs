using System;
using System.IO;
using System.Reflection;

namespace DevelopmentActivity.Producer.Shared
{
    public enum RequestResult
    {
        OK,
        Error,
        NoChange,
        Change
    }
}
